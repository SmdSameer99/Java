package Recursion;

public class StringRemoveChars {
    public static void main(String[] args) {
        
    }

    static void skipchar(String str, String skip) {
        if (str.isEmpty()) {
            System.out.println(str);
            return;
        }
        
        char ch = str.charAt(0);

        if (ch == )
    }
